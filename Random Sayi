#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main()
{
    int sayi,toplam=0;
    srand(time(NULL));

    for(int i=0;i<10;i++)
    {
        sayi=rand()%10+1;
        printf("%d. sayi :%d\n",i+1,sayi);
        toplam+=sayi;
    }
    printf("%d\n",toplam/10);

    return 0;
}
